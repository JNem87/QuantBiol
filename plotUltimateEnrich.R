
plotUltimateEnrich <- function(resSet, title = "Enrichment Result", xax, yax){

        # tmpResDFmerge <- tmpResDFmerge[order(-tmpResDFmerge$pvalue),]
        #
        # tmpResDFmergeLevels <- tmpResDFmerge$PATHNAME
        # ggplot(data=tmpResDFmerge, aes(x=factor(PATHNAME, levels = rev(tmpResDFmergeLevels)), y=pvalue)) +
        #         geom_bar(stat="identity") +
        #         coord_flip() +
        #         theme_bw() +
        #         theme(panel.grid.major = element_blank(), panel.grid.minor = element_blank()) +
        #         xlab("Significant Complexes")





}




# tmpResDFmerge$pvalue <- -log2(tmpResDFmerge$pvalue)
# x <- tmpResDFmerge[order(-tmpResDFmerge$pvalue),]





